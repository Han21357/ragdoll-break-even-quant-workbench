# 老布偶猫回本之路 · Python 依赖

# === 核心后端 ===
Flask>=3.0
flask-cors>=5.0
python-dotenv>=1.0
baostock>=0.8
requests>=2.31

# === 自研 Agent 链路（必装）===
langgraph>=1.2
langchain-core>=1.4

# === 数据处理 ===
pandas>=2.2
pydantic>=2.0

# === TradingAgents 集成（选装，需要 7 角色 Agent 时安装）===
# 安装后会拉入 200+ 依赖（langchain 全家桶、yfinance、opentelemetry 等）
# 如果只用自研 5 层 Agent 链路，可以跳过
# tradingagents>=0.7
# yfinance>=1.5
# langchain-openai>=1.3
